import 'package:flutter/material.dart';

class HMColors {
  HMColors._();

  /// ================ APP BASIC COLORS ================

  /// --- IMDB-inspired gold ---
  static const Color primary = Color(0xFFF5C518);
  static const Color secondPrimary = Color(0xFF3B4CCA);

  /// --- Deep navy (IMDB-inspired) ---
  static const Color secondary = Color(0xFF032541);

  /// --- Netflix-inspired red ---
  static const Color accent = Color(0xFFE50914);

  /// ================ TEXT COLORS ================

  /// --- White for dark backgrounds ---
  static const Color textPrimary = Color(0xFFFFFFFF);

  /// --- Light gray for secondary text ---
  static const Color textSecondary = Color(0xFFB3B3B3);
  static const Color textLight = Color(0xFF9A9BB2);
  static const Color textWhite = Colors.white;

  /// ================ BACKGROUND COLORS ================

  static const Color light = Color(0xFFF6F6F6);
  static const Color dark = Color(0xFF121212);

  /// --- black for movie cards ---
  static const Color blackBackGround = Color(0xFF0A0A0A);

  /// --- Dark gray background ---
  static const Color primaryBackground = Color(0xFF1F1F1F);

  /// --- Background Container Colors ---
  static const Color lightContainer = Color(0xFFF6F6F6);

  /// --- Slightly lighter than background ---
  static const Color darkContainer = Color(0xFF2A2A2A);

  /// ================ BUTTON COLORS ================

  /// --- Gold for primary actions ---
  static const Color buttonPrimary = Color(0xFFF5C518);

  /// --- IMDB-inspired teal ---
  static const Color buttonSecondary = Color(0xFF01B4E4);

  /// --- Darker disabled state ---
  static const Color buttonDisabled = Color(0xFF4F4F4F);

  /// --- Darker red variant ---
  static const Color buttonAlt = Color(0xFF8C1F28);

  /// ================ BORDER COLORS ================

  static const Color borderPrimary = Color(0xFF2A2A2A);
  static const Color borderSecondary = Color(0xFF3A3A3A);

  /// ================ ERRORS AND VALIDATION COLORS ================

  static const Color error = Color(0xFFD32F2F);

  /// --- Slightly darker green ---
  static const Color success = Color(0xFF2E7D32);

  /// --- Amber for warnings ---
  static const Color warning = Color(0xFFFF8F00);

  /// --- Slightly adjusted blue ---
  static const Color info = Color(0xFF0288D1);

  /// ================ NEUTRAL SHADES ================

  static const Color black = Color(0xFF000000);
  static const Color darkerGrey = Color(0xFF121212);
  static const Color darkGrey = Color(0xFF2A2A2A);
  static const Color grey = Color(0xFF4F4F4F);
  static const Color softGrey = Color(0xFF757575);
  static const Color lightGrey = Color(0xFFB3B3B3);
  static const Color white = Color(0xFFFFFFFF);

  /// --- IMDB-inspired blue ---
  static const Color blue = Color(0xFF01B4E4);

  /// --- Slightly lighter than background ---
  static const Color searchBar = Color(0xFF2A2A2A);

  /// ============ INDICATORS =================

  static const Color loadingLightIndicator = Color(0xFFE94560);
}
